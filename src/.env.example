# Herman Kwayu Portfolio - Environment Variables
# Copy this file to .env.local for local development

# =============================================================================
# REQUIRED VARIABLES (CRITICAL FOR FUNCTIONALITY)
# =============================================================================

# Supabase Configuration (Required for backend functionality)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
SUPABASE_DB_URL=your_supabase_database_url

# Email Service (Required for newsletter and contact form)
RESEND_API_KEY=your_resend_api_key

# =============================================================================
# OPTIONAL VARIABLES (ADD IF USING THESE SERVICES)
# =============================================================================

# Payment Processing (if implementing paid features)
DPO_COMPANY_TOKEN=your_dpo_company_token
DPO_SERVICE_TYPE=your_dpo_service_type

# WhatsApp Integration (if implementing WhatsApp features)
WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token
WHATSAPP_PHONE_NUMBER_ID=your_whatsapp_phone_number_id

# Twilio SMS Service (if implementing SMS features)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=your_twilio_phone_number

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Admin Configuration
ADMIN_SESSION_SECRET=your_secure_admin_session_secret_minimum_32_characters

# Environment
NODE_ENV=development
VITE_APP_ENV=development

# Application URLs (Update with your actual domain for production)
VITE_APP_URL=http://localhost:5173
VITE_API_URL=https://your-supabase-project.supabase.co/functions/v1

# =============================================================================
# FEATURE FLAGS
# =============================================================================

# Analytics & Performance Monitoring
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_PERFORMANCE_MONITORING=true

# Development Features
VITE_ENABLE_DEBUG_MODE=false
VITE_ENABLE_ERROR_REPORTING=true

# =============================================================================
# LOCAL DEVELOPMENT TIPS
# =============================================================================

# 1. Copy this file to .env.local: cp .env.example .env.local
# 2. Replace all "your_*" placeholders with actual values
# 3. VITE_ prefixed variables are exposed to the frontend
# 4. Non-VITE_ prefixed variables are server-side only
# 5. Never commit .env.local to version control
# 6. For production deployment, set these in Vercel dashboard

# =============================================================================
# SECURITY NOTES
# =============================================================================

# - NEVER commit real API keys to version control
# - Use strong, unique passwords for admin sessions
# - Rotate API keys regularly
# - Keep service role keys secure (backend only)
# - Monitor API usage for unusual activity